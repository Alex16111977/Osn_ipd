# 📝 Инструкция по созданию GitHub репозитория для проекта Osn_ipd

## 🌐 Шаг 1: Создание репозитория на GitHub

1. **Откройте браузер** и перейдите на страницу: https://github.com/new
   
   Или:
   - Зайдите на https://github.com/Alex16111977?tab=repositories
   - Нажмите зеленую кнопку **"New"** справа вверху

2. **Заполните информацию о репозитории:**

   - **Repository name:** `Osn_ipd`
   - **Description:** `AI-система для автоматизации классификации основных средств по кодам УКТ ЗЕД`
   - **Visibility:** Выберите `Public` или `Private` (по вашему усмотрению)
   - **Initialize this repository with:**
     - ❌ **НЕ ставьте галочку** "Add a README file" (у нас уже есть README.md)
     - ❌ **НЕ добавляйте** .gitignore (мы создадим свой)
     - ❌ **НЕ выбирайте** лицензию (можно добавить позже)

3. Нажмите кнопку **"Create repository"**

---

## 💻 Шаг 2: Инициализация локального репозитория

Откройте **командную строку** (cmd) и выполните следующие команды:

### 2.1. Перейдите в папку проекта:
```cmd
cd /d F:\AiKlientBank\Osn_ipd
```

### 2.2. Инициализируйте Git репозиторий:
```cmd
git init
```

### 2.3. Добавьте все файлы:
```cmd
git add .
```

### 2.4. Создайте первый коммит:
```cmd
git commit -m "Initial commit: AI система классификации основных средств УКТ ЗЕД"
```

### 2.5. Переименуйте ветку в main:
```cmd
git branch -M main
```

### 2.6. Добавьте удаленный репозиторий:
```cmd
git remote add origin https://github.com/Alex16111977/Osn_ipd.git
```

### 2.7. Отправьте код на GitHub:
```cmd
git push -u origin main
```

---

## 🔐 Если попросит авторизацию:

GitHub может запросить аутентификацию. Используйте один из способов:

### Способ 1: Personal Access Token (рекомендуется)

1. Перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Дайте название токену: `Osn_ipd Token`
4. Выберите срок действия: `No expiration` или укажите срок
5. Отметьте галочками:
   - ✅ `repo` (все подпункты)
   - ✅ `workflow`
6. Нажмите **"Generate token"**
7. **Скопируйте токен** (он больше не будет показан!)
8. Используйте токен вместо пароля при git push

### Способ 2: GitHub CLI

```cmd
# Установите GitHub CLI: https://cli.github.com/
gh auth login
```

---

## 📋 Шаг 3: Проверка

После успешной загрузки:

1. Откройте в браузере: https://github.com/Alex16111977/Osn_ipd
2. Убедитесь, что все файлы загружены
3. Проверьте, что README.md отображается на главной странице

---

## 🎯 Быстрая команда (после создания репозитория на GitHub):

Если хотите выполнить всё одной командой, скопируйте и вставьте в cmd:

```cmd
cd /d F:\AiKlientBank\Osn_ipd && git init && git add . && git commit -m "Initial commit: AI система классификации основных средств УКТ ЗЕД" && git branch -M main && git remote add origin https://github.com/Alex16111977/Osn_ipd.git && git push -u origin main
```

---

## 🔄 Дальнейшая работа с репозиторием

### Добавление изменений:
```cmd
git add .
git commit -m "Описание изменений"
git push
```

### Проверка статуса:
```cmd
git status
```

### Просмотр истории:
```cmd
git log --oneline
```

### Клонирование на другой компьютер:
```cmd
git clone https://github.com/Alex16111977/Osn_ipd.git
```

---

## 📞 Если что-то пошло не так:

### Ошибка: "remote origin already exists"
```cmd
git remote remove origin
git remote add origin https://github.com/Alex16111977/Osn_ipd.git
```

### Ошибка: "failed to push"
```cmd
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Сбросить всё и начать заново:
```cmd
rmdir /s /q .git
# Затем начните с Шага 2.2
```

---

## ✅ Что будет загружено:

- ✅ README.md - описание проекта
- ✅ requirements.txt - зависимости Python
- ✅ Documents/ - документация по УКТ ЗЕД
- ✅ input/ - входные CSV файлы
- ✅ output_ai/ - результаты работы системы
- ✅ Promt/ - промпты для AI
- ❌ .venv/ - не будет загружено (виртуальное окружение)
- ❌ __pycache__/ - не будет загружено (кэш Python)

---

## 🎉 Готово!

После выполнения всех шагов ваш проект будет доступен по адресу:
**https://github.com/Alex16111977/Osn_ipd**

Теперь вы можете делиться проектом с коллегами и управлять версиями кода!

---

**Дата создания инструкции:** 26 ноября 2025
**Автор:** Alex16111977
